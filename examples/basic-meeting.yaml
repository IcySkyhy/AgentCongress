meeting:
  id: architecture-review
  initial_speaker: architect
  initial_addressee: reviewer
  execution_mode: continuous
  agents:
    - id: architect
      role: system architect
      capability_tags: [architecture, interfaces]
    - id: reviewer
      role: skeptical code reviewer
      capability_tags: [security, testing]
    - id: implementer
      role: implementation specialist
      capability_tags: [python, git]
  # 可选：模型支持讨论轮的提供商设置（CLI 参数优先）
  # discussion:
  #   provider: openai-chat          # openai-chat | openai-responses | anthropic
  #   model: gpt-4o-mini
  #   api_key_env: OPENAI_API_KEY
  #   listener_mode: silent          # silent | llm（llm = 工具调用听众评估器）
  #   max_tool_rounds: 8
